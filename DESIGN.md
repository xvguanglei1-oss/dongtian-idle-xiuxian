# 洞天·挂机修仙 —— 设计语言 DESIGN.md

> 版本 1.0 · 定稿于 2026-09-08（线上 v0.4.x 迭代收敛）
> 本文件是**唯一视觉权威**：任何新功能、新组件、新页面，先读本文件，再动手。

---

## 0. 一句话定位

**「一册夜行的墨线手札」**：所有 UI 都是"纸墨上随手画出的物件"——半透明的墨块、手绘的曲线轮廓、盖下去的朱砂印。核心追求是 Messenger 那套基因：**不完美(imperfect) + 克制的情绪(calm but weird)**。绝不允许出现"工业感"的直角/正圆角/高饱和霓虹。

## 1. 色彩纪律（一砚墨 + 四色）

> 任何元素取色**只能**从下表取。宁少勿多。

| 名称 | Token | 值 | 用途 |
|---|---|---|---|
| 夜色底 | `--bg` | `#0a0d16` | 页面背景（洞天外的虚空） |
| 墨卡底 | `--panel` | `rgba(12,17,31,.55~.78)` | 墨块填充（**一律半透明**） |
| 纸白 | `--ink` | `#e9e2d0` | 主文字 |
| 淡墨 | `--ink-dim` | `#a49a84` | 次级文字 |
| 旧金 | `--gold` | `#c9a86a` | 数值/描边/稀有，**不亮的金** |
| 朱砂 | `--cinnabar` | `#b9342a` | **全界面唯一高饱和**，只给关键动作 |
| 黛青 | `--jade` | `#86b5a2` | 灵石/生命/分身类点缀，克制使用 |

**铁律**：
- 文字不直接用纯白？可以（`#fff` 用于高亮字幕），但大面积文字用纸白/淡墨。
- 霓虹色（亮蓝/荧光绿/品红）**禁止**。曾经出现的 `#7fe0ff/#8fb6ff` 一律降为纸白系或黛青。
- 背景永不压黑到看不清；前景用亮描边或投影与背景分开。

## 2. 半透明墨块（本语言的核心质感）

- **所有**容器皮肤填充 alpha ∈ [0.45, 0.6]（约半透明），透出星空/背景，保持"墨影"而非"色块"。
- 形状由 **不透明描边（stroke alpha ≥ .7）+ 实色文字** 勾勒——所以"半透明但看得清"。
- 案例：按钮墨块 `fill: rgba(...,.5); stroke: rgba(...,.85); 文字 #fff/#ecd9a8`。

## 3. 形状语言：只用 SVG path 曲线，禁止 CSS 大圆角

- CSS `border-radius` 只能切出直线+圆弧 → **与手绘语言冲突**。容器一律用 **SVG path（贝塞尔 C/Q）** 画"墨迹/纸笺"轮廓。
- 用 CSS `background-image: url("data:image/svg+xml;...")` + `background-size:100% 100%` 让**墨形随内容伸缩**（内容撑框，不是框裁内容）。
- 若元素在 JS 内动态生成、不便塞 SVG → 使用"透明底 + 描边线/竖线/渐变"等轻方案，**禁止**退化成圆角矩形底。

### 现成的墨形库（改路径即换形态）

| 用途 | viewBox | 说明 |
|---|---|---|
| 按钮 | `0 0 200 60` | 两颗主钮各有独立 path |
| 大徽章（境界） | `0 0 200 120` | 上窄下宽的墨简，可容纳 3 行 |
| HUD 小胶囊（灵石/修为/云存） | `0 0 200 40` | 上下轻微波浪 |
| 弹层卡（离线/修行录） | `0 0 200 150` | 大纸形 |
| 日志墨条 | `0 0 200 44` | **已废弃**——日志改播报墙（见 §6） |

**防呆**：新组件先问"要不要 SVG 皮"，不要顺手写 `border-radius:12px`。

## 4. 按钮规范（含三态）

结构（JS 更新文字时**只动 `.label`**，严禁 `textContent` 整锅替换）：
```html
<button class="btn [primary|ready]">
  <svg class="skin" viewBox="0 0 200 60" preserveAspectRatio="none"><path class="ink" d="…"/></svg>
  <span class="label">☯ 立即突破</span>
</button>
```

| 状态 | 触发 | 墨块 | 文字 |
|---|---|---|---|
| 可点 | 默认 | 半透明墨金 `.5`（primary 朱砂 `.5`） | `#ecd9a8` / 暖白 |
| 激活 | `.ready`（如可渡劫） | 亮朱砂 `.55` + 红光脉动 keyframes `inkBeat` | `#fff0da` |
| 熄墨 | `:disabled` | 灰蓝 `.55` 亮描边（**可见，非透明消失**） | `#dfe6f5` |

细节：描边 `vector-effect: non-scaling-stroke`；`filter: drop-shadow` 做投影；`:active` 盖印式压扁（rotate+scale+.92）；字体用楷体栈；圆角≈0、无 backdrop blur 残留。

## 5. 字体

```css
--font-brush: "Kaiti SC","STKaiti","KaiTi","DFKai-SB","BiauKai",serif;  /* 标题/按钮/境界名/印章 */
--font-body:  "Noto Serif SC","Songti SC","STSong","SimSun",serif;       /* 正文/日志 */
```
- 按钮、境界大字、印章落款、`h3/h4/h5` → brush
- 正文、日志 → body；字重 400–600；数字可用全角/衬线保古意
- **长文案永远允许换行/断词**（`overflow-wrap:anywhere`），不许溢出墨形

## 6. 播报墙（日志系统，替代逐条墨条）

- 左右两栏：左=分身（灰 `#ccd6ea`、更快），右=主线（**纯白 `#fff`**、适中）
- **无深色遮罩**；右栏允许一层**极淡灰罩** `rgba(210,220,240,.10)` 提高对比
- 位置：`bottom:124px` 左右，高度 `min(34vh,300px)`，不遮挡底部按钮
- 节奏：主线 12s（淡入→驻留→淡出）、分身 6.5s；**超出条数上限立即让最旧一条淡出**（滚动感，禁止全部直出堆叠）
- 文字排版：右侧 `text-align:right` 靠右、左侧靠左，行高 1.55

## 7. 版本号与防缓存

- 主题独立文件 `dt-theme.css?v=X.Y.Z`；JS 改动后 bump `game.js?v=X.Y.Z`
- 版本角标：左上 `剧情纲目` 旁 `.ver-tag` + `<title>` 同步
- **每次视觉/逻辑改动必须升版本**（用户手机缓存难缠，别赌 CDN）

## 8. 历史坑清单（回退检查用）

1. **JS 更新按钮文字禁止 `textContent`**——会把内嵌 SVG 皮肤整个删掉（导致"按钮全透明只见字"的历史 bug）
2. 给容器换墨形皮后，**务必清掉**旧 `border/border-radius/background-color/box-shadow/backdrop-filter`，否则圆角矩形底"借尸还魂"
3. 墨形背景 `background-size:100% 100%` → 会随内容伸缩：**要给足 padding**，文字才不会跑出墨形
4. `:disabled` 不能做成"接近背景色"，否则看不见；用亮一档的灰蓝/灰紫
5. 图标勿用平台 emoji 当关键资产（🪙 会被画成金币）——用 SVG 自制（灵石=青玉菱形）
6. 布局用 flex 容器时注意 `align-items`（默认 stretch 会让墨条拉满容器，需要贴内容宽用 start/end）

## 9. 语言检查（提交前自问）

- [ ] 有没有出现正圆角矩形/直角白底？
- [ ] 有没有出现霓虹高饱和色（除朱砂）？
- [ ] 墨块是否半透明？描边与文字是否清晰？
- [ ] 文字有没有溢出墨形？
- [ ] 按钮三态（可点/激活/熄墨）是否都看得清？
- [ ] JS 改 DOM 有没有整锅替换？
- [ ] 版本号 bump 了吗？
