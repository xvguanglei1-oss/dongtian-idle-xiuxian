# v1 问题排查清单

> 目的：给 Agent 的第一轮代码核查与修改任务。
>
> 范围：只处理前端仓库当前可见代码与资源。**暂不检查后端实现、后端数据一致性或服务端安全策略**，因为当前仓库无法提供后端实现。
>
> 原则：先核查、再修改；不要为了“重构”而重写已经正常工作的系统。所有修改必须保持现有玩法、存档兼容性和 UI 功能不被无故破坏。

## P0 — 必须优先核查

### 1. 境界 / 段位模型一致性

当前 `game.js` 的 `BIGS` 实际包含：
- 凡人：1 段
- 炼气：13 层
- 筑基、结丹、元婴、化神、炼虚、合体、大乘、渡劫、真仙、天仙：各 4 段

即当前模型实际为 **54 段**。

但代码中存在 `TOTAL_SEGS` 与“30段”相关的旧注释/旧模型痕迹。

Agent 要求：
- 全仓搜索 `TOTAL_SEGS`、`SEG_META`、`BIGS`、境界段数相关常量。
- 确认 `TOTAL_SEGS` 是否参与实际计算、进度、存档、里程碑、UI 或突破逻辑。
- 如果只是废弃常量/旧注释，删除或修正，避免以后误导维护者。
- 如果仍参与运行时逻辑，必须统一到当前 54 段模型。
- 禁止为了修复而改变现有境界设计本身。

### 2. 存档数据健壮性

重点检查：
- `localStorage`
- `JSON.stringify / JSON.parse`
- `lz-string`
- 存档保存/读取入口
- 存档版本号
- 老版本字段缺失时的默认值
- 数值字段出现 `NaN`、`Infinity`、负数或异常大值时的处理
- JSON/LZString 解压失败时是否会直接把坏存档覆盖掉正常存档

要求：
- 不改变正常玩家的存档结构和玩法。
- 对关键数值增加有限度的合法性兜底。
- 读取失败必须优先保护现有存档，而不是无条件初始化覆盖。
- 如果项目已有版本迁移机制，补齐当前 v1.7.17 的迁移路径；如果没有，增加最小可用的版本兼容层。

### 3. 离线收益 / 时间计算

重点检查完整链路：
`最后保存时间 → 当前时间 → 离线时长 → 修为/灵石/资源收益 → 写回存档`

重点排查：
- 系统时间倒退
- 修改系统时间获得异常收益
- 离线时长没有上限
- `Date.now()` 计算出现负数
- 超长离线导致收益溢出
- 收益计算产生 `NaN / Infinity`
- 重复结算同一段离线时间
- 保存前后时间戳更新顺序错误
- 切后台/恢复前台时重复发放收益

要求：
- 正常离线收益逻辑保持不变。
- 对异常时间差设置合理上限/下限，并保证不会破坏正常长时间离线玩家。
- 离线结算必须做到一次时间窗口只结算一次。
- 关键资源最终写回前必须保证为有限数值。

## P1 — 核查后直接修

### 4. 聚灵阵经济曲线

重点检查：
`ARRAY_COST = 900 * Math.pow(lv + 1, 2.55)`

要求：
- 核查当前聚灵阵升级成本是否存在明显的断档、溢出或无法升级问题。
- 检查最高等级附近的数值是否超过 JavaScript Number 安全整数精度或造成 UI 显示异常。
- 不在本轮擅自改经济平衡，只修复明确的数值安全问题。

### 5. 音频播放条件

检查 SFX/BGM 的播放条件，特别关注是否存在把音效播放与 `stage === 1` 等战斗阶段状态错误耦合的情况。

要求：
- 音效播放条件应该由“音效开关 + 页面可见性 + 浏览器播放策略”等因素决定。
- 不应因为战斗 stage 变化导致本应播放的通用 UI/系统音效失效。
- 不改变用户主动关闭音效的行为。

### 6. 音乐资源实际引用

仓库目前存在多首 BGM 资源。

要求：
- 全仓搜索 `bgm.mp3`、`bgm2.mp3`、`bgm3.mp3`、`bgm4.mp3` 的实际引用。
- 区分“实际使用”“备用资源”“完全未引用”。
- 本轮不要直接删除资源；只有确认完全无引用且不会被动态路径加载时，才可以提出清理建议。
- 重点避免把 17MB 左右音乐资源全部随首屏加载。

## P1 — 前端性能

### 7. 深空背景移除 Three.js 依赖

当前深空背景使用 Three.js WebGL，同时仓库还有 Canvas 2D 特效系统。

结论：**深空背景不需要 Three.js。**

要求 Agent：
- 移除深空背景对 Three.js 的运行时依赖。
- 优先使用现有 Canvas 2D / CSS / 静态背景资源实现相同或接近的视觉效果。
- 不要因为“去 Three.js”而顺手重做整个游戏 UI。
- 保留核心视觉层次：星空、星点、月亮/核心视觉、轻量动态粒子等。
- 移除后确认不存在 `THREE`、`three.module.js` 的运行时引用。
- 如果 `three.module.js` 只为该背景服务，完成验证后可以标记为可清理资源，但本轮不要未经验证直接删除。

### 8. 双渲染循环性能

检查：
- Three.js RAF
- `fx2d.js` RAF
- DOM/CSS 动画
- 图片动画
- 战斗特效

目标：减少重复 `requestAnimationFrame` 和无意义的后台持续渲染。

要求：
- 页面不可见时停止或显著降低非必要动画。
- 不允许为了性能把战斗表现砍掉。
- 移除 Three.js 后重新确认是否还存在多个 RAF 长期同时运行。

## P1 — UI / 设计规范漂移

### 9. `DESIGN.md` 与 `index.html` 实际样式不一致

`DESIGN.md` 明确要求：
- 避免普通圆角矩形卡片。
- 使用 SVG path / 墨线 / 书页、卷轴等东方视觉语言。
- 淘汰旧的霓虹蓝/绿/洋红风格。

但 `index.html` 仍存在大量旧式：
- `border-radius: 999px`
- `border-radius: 14px / 18px`
- `backdrop-filter: blur(...)`
- `#7fe0ff`
- `#8fb6ff`

要求：
- 先确认这些样式实际作用于哪些组件。
- 优先修复仍暴露给玩家的主要 UI。
- 不要一次性大规模重做布局。
- 重点解决“设计规范写了一套、实际 UI 还是旧风格”的问题。
- 新增/修改组件必须遵守 `DESIGN.md`，不能继续产生新的普通圆角卡片。

## P2 — 代码结构与维护性

### 10. `game.js` 单文件过大

当前 `game.js` 约 250KB+，同时承担：
- 境界
- 修炼
- 聚灵阵
- 战斗
- 怪物
- 角色/分身
- 云游
- 离线收益
- 存档
- 剧情
- 里程碑
- UI
- 音频
- 突破/渡劫
- 装备
- 动画等多个系统。

本轮**不要进行大规模重构**。

只要求：
- 找出本轮实际修改涉及的高风险函数。
- 避免在一个函数里继续堆叠新的全局状态。
- 修 bug 时尽量做局部、可回滚修改。
- 如果发现明显重复函数/死代码，只记录，不为了“整洁”而大拆文件。

## 11. 全局数值安全扫描

对资源、修为、寿元、灵石、聚灵阵、战斗伤害、怪物 HP、倍率、收益等关键数值检查：
- `NaN`
- `Infinity`
- `-Infinity`
- `undefined`
- 字符串误参与数学运算
- 负数资源
- 超大数显示
- `Math.pow` / 指数增长溢出

要求：
- 对关键状态统一使用有限数值兜底。
- 不要用粗暴的全局 `Math.max(0, ...)` 覆盖所有逻辑；必须区分资源、倍率、时间、战斗数值。

## P2 — 浏览器生命周期

### 12. 前后台切换 / 可见性

检查：
- `visibilitychange`
- `pagehide / pageshow`
- `blur / focus`
- `beforeunload`

重点确认：
- 后台不会疯狂跑动画。
- 回前台不会重复结算。
- 页面关闭前的保存时机合理。
- 音频恢复符合浏览器自动播放限制。

## P2 — 网络 / 外部资源

### 13. 前端外部请求与密钥扫描

虽然本轮不检查后端，但仍需扫描前端仓库是否存在：
- API Key
- Token
- Secret
- Authorization Bearer
- 明文密码
- 私有接口地址
- 测试密钥

要求：
- 如果发现疑似秘密，不要把具体值写入报告或日志。
- 只报告文件位置、变量名和风险类型。
- 前端代码里的任何“秘密”默认都视为可被用户看到，不能当真正 Secret 使用。

## 14. 外部资源失败兜底

检查图片、音乐、字体、第三方脚本加载失败时：
- 是否导致整个游戏初始化失败。
- 是否出现 JS 未捕获异常后核心系统停止。
- 是否有必要的 fallback。

重点关注：
- `three.module.js`
- 音频
- `lz-string.min.js`
- 远程资源

## 验收标准

Agent 完成后必须给出：

1. **发现的问题列表**：P0/P1/P2 分级。
2. **实际修改文件列表**。
3. **每个修改的原因**。
4. **没有修改的疑点**，说明为什么暂不动。
5. **存档兼容性结论**。
6. **离线收益时间作弊/重复结算结论**。
7. **Three.js 移除后的引用扫描结果**。
8. **`TOTAL_SEGS`/54段模型一致性结论**。
9. **`NaN/Infinity` 扫描结论**。
10. **前端密钥扫描结论**。
11. 至少执行一次完整的浏览器运行/构建级验证（如果项目没有构建系统，则至少进行静态语法检查 + 实际打开运行）。

## 修改边界

- 不改后端。
- 不重新设计整个游戏。
- 不擅自改变境界数量、主要经济平衡、核心玩法。
- 不删除资源，除非先确认无引用。
- 不为了代码“漂亮”进行大规模重构。
- 不改变已有存档格式，除非增加向后兼容迁移。
- 所有 bug 修复以“最小改动、可验证、可回滚”为原则。

## v1 目标

**先把明显的结构性问题、数值安全问题、存档/离线结算风险、Three.js 性能负担和设计规范漂移处理掉。**

这一轮不是重做游戏，而是把现有版本从“能跑”推进到“稳定、可维护、不会因为边界条件突然坏掉”。
