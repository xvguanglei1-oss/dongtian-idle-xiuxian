# V1 前端问题排查报告（复审版）

> 复审日期：2026-09-10  
> 当前版本：v1.7.19  
> 范围：仅检查公开仓库可见的前端代码与资源。**不检查后端实现、后端数据一致性或服务端安全策略。**  
> 复审原则：以当前仓库实际代码为准，不以 Agent 自述结论为准；已确认修复的问题不再重复列入待办。

## 一、V1.7.19 已确认完成

### 1. 境界段数模型

当前 `BIGS` 为：凡人 1 段、炼气 13 层，其余 10 个大境各 4 段，总计 **54 段**。

`TOTAL_SEGS` 当前已明确计算为 54，不再保留原先“30 段”的旧值/旧注释问题。

**结论：通过。**

### 2. 存档基础数值兜底

当前已增加 `fin()` 有限数值检查，以及 `adopt()` 存档清洗逻辑；装备倍率等关键字段缺失/异常时有默认值回退。

**结论：基础 NaN / Infinity 污染链已经处理，但仍需要后续做一次实际坏档注入测试，不能仅凭静态代码宣布 100% 通过。**

### 3. 离线收益基础保护

当前存在 `OFFLINE_CAP = 48 * 3600`，离线收益最终对修为、灵石进行有限非负值兜底，并使用 `_lastTs0 / _settledTs` 避免启动时立即刷新 `lastTs` 导致真实离线区间被吞掉。

**结论：重复结算的基础保护和异常数值保护已明显改善；但本地系统时间作弊仍未真正解决，见 P0。**

### 4. fx2d 后台渲染保护

`fx2d.js` 已加入可见性运行守卫，后台不会继续无条件推进 RAF。

**结论：通过。**

### 5. 装备面板样式局部修正

v1.7.19 已对装备面板做局部视觉调整，提交记录显示已处理圆圈徽章等问题。

**结论：局部完成；整体设计规范仍有漂移，见 P1。**

---

# 二、当前真正需要继续处理的问题

## P0 — 先修

### P0-1：本地离线时间仍可被系统时间反复推进

当前逻辑虽然把单次离线收益限制在 48 小时，并对时间倒退/短时间差做了处理，但离线基准仍然建立在客户端 `Date.now()` 上。

这意味着攻击方式仍然成立：

1. 正常保存。
2. 手工把设备系统时间向未来调整。
3. 重新进入游戏获得最多 48 小时离线收益。
4. 结算后再次把系统时间向未来推进。
5. 可以重复制造未来时间窗口。

48 小时上限只能限制**单次窗口大小**，不能阻止**连续时间跳跃**。

**要求：**
- 不要假装客户端可以彻底防时间作弊；纯前端只能降低风险。
- 设计并实现明确的“时间回拨/异常跳跃”状态记录。
- 至少保证同一设备本地不能通过简单反复改时间无限刷离线收益。
- 正常跨天、长时间离线不能被误判成作弊。
- 不修改正常收益倍率和核心经济平衡。
- 如果最终仍无法在纯前端可靠解决，必须在报告中明确标记为“客户端不可彻底防护”，不要声称已解决。

### P0-2：存档损坏保护需要实际验证

当前 `adopt()` 和 `fin()` 已经存在，但仍需验证以下场景：

- 完全随机字符串。
- LZ 解压失败。
- JSON 合法但字段缺失。
- 关键资源为 `NaN` / `Infinity` / `-Infinity`。
- 装备数组损坏。
- `lastTs` 损坏。
- journal 超大。
- 正常旧档加载后保存。

**要求：**
- 坏档不能把正常存档无条件覆盖成新档。
- 坏档测试失败时只修最小范围。
- 明确给出“坏档 → 保护 / 回退 / 初始化”的实际行为。

---

# 三、P1 — 当前代码已经暴露的明确问题

## P1-1：Three.js 实际仍然存在，V1 要求并未完成

这是本轮最明确的未完成项。

当前仓库仍存在 `bg.js`，其中仍然：

```js
import * as THREE from 'three';
```

并且 `bg.js` 仍完整使用 `THREE.WebGLRenderer`、`THREE.Scene`、`THREE.BufferGeometry`、`THREE.ShaderMaterial` 等 WebGL 管线。

仓库当前树中也仍然存在 `three.module.js`。

因此不能把“fx2d 后台暂停”或“减少性能负担”当成“Three.js 已移除”。

**要求：**
- 深空背景改为 Canvas 2D / CSS / 静态背景方案。
- 删除深空背景运行时对 Three.js 的依赖。
- 全仓确认不存在运行时 `THREE` / `three.module.js` 引用。
- 确认 `index.html` 不再加载对应 Three.js 背景入口。
- 验证通过后，再决定是否删除 `three.module.js` 和 `bg.js`。
- 不要顺手重做游戏主体视觉。

**状态：未完成，P1。**

## P1-2：音效仍被 `stage === 1` 强耦合

当前 `game.js` 中的 `sfxAudible()` 仍然是：

```js
if (document.visibilityState === "hidden") return false;
return stage === 1;
```

因此“页面可见”并不是唯一播放条件，`stage` 仍然决定所有单发音效能否播放。

这与原 V1 要求“不要让通用音效错误依赖战斗 stage”并不一致。

**要求：**
- 页面不可见时静音是合理的。
- 用户关闭音效时静音是合理的。
- 浏览器 AudioContext 未激活时应等待用户手势恢复。
- 但通用 UI 音效、系统音效不应因为战报横幅 `stage` 状态而全部失效。
- 战斗专用音效可以有自己的演出窗口限制，但必须与通用 SFX 条件分开。

**状态：部分处理，仍需修。**

## P1-3：DESIGN.md 与实际 index.html 仍存在明显漂移

当前 `index.html` 仍可见：

- `border-radius: 999px`
- `border-radius: 14px / 18px`
- `backdrop-filter: blur(...)`
- 旧式霓虹蓝系颜色，如 `#7fe0ff`、`#8fb6ff`
- 大量通用圆角 chip / card 视觉。

而 `DESIGN.md` 明确要求减少普通圆角矩形、避免旧霓虹视觉，并使用墨线、书页、卷轴、SVG path 等东方视觉语言。

v1.7.19 对部分装备面板已经做了调整，但不能因此认为整个 UI 已完成规范统一。

**要求：**
- 不大改布局。
- 先处理仍直接暴露给玩家的主要 HUD / 弹窗 / 卡片。
- 圆角不是“一刀切全部删除”，但普通胶囊按钮、圆角卡片、模糊玻璃卡片不能继续无控制增长。
- 保持现有可用性和移动端点击区域。

**状态：部分完成，仍需修。**

---

# 四、P1 — 需要验证而不是凭感觉修改

## P1-4：多 BGM 资源是否浪费首屏/仓库体积

当前 `assets/music` 仍有：

- `bgm.mp3`
- `bgm2.mp3`
- `bgm3.mp3`
- `bgm4.mp3`

总资源体积约 18 MB。

当前已确认 `game.js` 主 BGM 路径使用 `assets/music/bgm.mp3`，但不能仅凭这一处就武断删除其他文件。

**要求：**
- 全仓确认四个文件的静态引用与动态路径引用。
- 如果只有 `bgm.mp3` 被实际加载，其余资源可以列为后续清理候选。
- 本轮重点确认“是否会被首屏预加载”，不要只看文件是否存在。
- 未验证前禁止删除。

## P1-5：聚灵阵成本曲线需要数值验证

当前：

```js
const ARRAY_COST = lv => 900 * Math.pow(lv + 1, 2.55);
```

目前没有证据表明它已经发生 Number 溢出，因此本轮**不要擅自改经济曲线**。

**要求：**
- 计算实际游戏允许的最高等级范围。
- 检查最高等级成本、累计成本是否超过安全整数精度。
- 检查 UI 格式化是否异常。
- 如果没有实际问题，标记通过，不要为了“看起来危险”而改数值。

---

# 五、P2 — 工程问题

## P2-1：game.js 仍然过于集中

`game.js` 仍承担境界、修炼、战斗、装备、云游、离线、存档、UI、音频等大量职责。

**本轮不要大拆。**

只要求：后续修 bug 时继续保持局部修改，避免继续制造新的全局状态和重复逻辑。

## P2-2：版本号 / cache busting 仍靠手工同步

当前代码仍存在：

- `GAME_VER = "v1.7.19"`
- `game.js?v=...`
- HTML 标题 / UI 版本号

这些值存在手工同步风险。

**要求：**
- 本轮不必引入构建系统。
- 只需避免出现“显示版本和实际脚本缓存版本不一致”的情况。
- 后续如果项目继续增长，再考虑单一构建版本来源。

## P2-3：生命周期事件需要回归测试

当前已有 `setInterval(save, 8000)`、`pagehide`、`visibilitychange` 等机制。

静态结构看起来已经有后台保存保护，但必须验证：

- 切后台 → 回前台。
- 快速连续切换。
- 关闭页面。
- 浏览器冻结/恢复页面。
- 回前台后是否重复离线结算。

**要求：**至少实际跑一次生命周期测试，不要只看代码。

---

# 六、明确暂不处理

以下项目本轮不要继续扩大范围：

1. **后端实现**：当前审查范围不包含后端。
2. **核心经济平衡**：除非发现明确的溢出/逻辑错误，不调整倍率和成长曲线。
3. **境界设计**：54 段模型已经统一，不重新设计境界。
4. **大规模代码重构**：不为了代码漂亮拆 `game.js`。
5. **全量 UI 重做**：只处理明确违反设计规范且影响当前体验的部分。
6. **资源删除**：先确认引用，再清理。

---

# 七、最终验收顺序

Agent 下一轮只按下面顺序处理：

### A. 必须完成

- [ ] P0-1：本地时间跳跃/重复离线收益风险处理或明确不可彻底防护。
- [ ] P0-2：坏档注入测试 + 存档保护验证。
- [ ] P1-1：彻底移除深空背景 Three.js 运行时依赖。
- [ ] P1-2：拆开通用 SFX 与 `stage` 的错误耦合。

### B. 完成后再处理

- [ ] P1-3：继续收敛 UI 与 DESIGN.md 的视觉规范漂移。
- [ ] P1-4：确认 BGM 实际加载链。
- [ ] P1-5：聚灵阵最高等级数值安全验证。
- [ ] P2-3：生命周期实际回归测试。

### C. 验收报告必须写清

最终只报告四种状态：

- **已修复**：代码和测试均能证明。
- **部分修复**：有代码处理，但仍存在明确缺口。
- **未修复**：当前代码仍存在问题。
- **无需修复**：经过代码/数值验证确认没有实际问题。

不要把“加了兜底”直接等同于“问题彻底解决”。

---

# V1 复审结论

当前 v1.7.19 **不是需要推倒重来的版本**。Agent 已经处理掉一批明显的数值和后台渲染问题，但本轮最关键的两个遗漏很明确：

1. **Three.js 仍然实际存在，深空背景移除任务没有完成。**
2. **SFX 仍然由 `stage === 1` 总开关控制，原先指出的音效耦合问题没有真正解除。**

另外，本地离线时间作弊只能在纯前端范围内降低风险，不能假装存在绝对安全方案；这一点下一轮必须把“能防到什么程度”说清楚。

因此下一轮不是继续撒网式排查，而是**集中把 P0-1、P0-2、P1-1、P1-2 四件事做完并实际验证**。
