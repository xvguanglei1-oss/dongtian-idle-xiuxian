# V1 前端问题排查报告（复审 + 性能版）

> 复审日期：2026-09-10  
> 当前版本：v1.7.19  
> 范围：仅检查公开仓库可见的前端代码与资源。**不检查后端实现、后端数据一致性或服务端安全策略。**  
> 原则：以当前仓库实际代码为准，不以 Agent 自述结论为准；已确认修复的问题不重复列为待办。

## 一、已确认完成

### 1. 境界段数模型
`BIGS` 当前为凡人 1 段、炼气 13 层、其余 10 个大境各 4 段，总计 **54 段**；`TOTAL_SEGS` 已统一为 54。

**状态：通过。**

### 2. 存档基础数值兜底
已有 `fin()` 有限数值检查、`adopt()` 存档清洗，以及装备倍率等关键字段默认值回退。

**状态：基础 NaN / Infinity 污染链已处理；仍需坏档注入实测。**

### 3. 离线收益基础保护
已有 `OFFLINE_CAP = 48 * 3600`，并使用 `_lastTs0 / _settledTs` 保护真实离线窗口，收益有有限非负兜底。

**状态：重复结算基础保护通过；本地时间作弊仍未解决，见 P0。**

### 4. fx2d 后台渲染保护
`fx2d.js` 已有可见性运行守卫，页面进入后台后停止 RAF。

**状态：通过。**

---

# 二、P0：必须先处理

## P0-1：本地时间跳跃仍可反复制造离线收益

当前单次离线收益虽然限制为 48 小时，但离线基准仍依赖客户端 `Date.now()`。玩家可以通过反复把系统时间推进到未来，连续制造新的离线窗口。

**要求：**
- 记录并识别明显的时间回拨、未来跳跃和异常连续跳跃。
- 至少阻止简单的“改时间 → 结算 → 再改时间 → 再结算”无限刷收益。
- 正常跨天、长时间离线不得误伤。
- 不改变正常收益倍率和经济曲线。
- 纯前端无法彻底防护时必须明确写明“客户端不可彻底防护”，不要宣称完全解决。

## P0-2：坏档保护必须实际测试

测试至少覆盖：
- 随机字符串；
- LZ 解压失败；
- JSON 合法但字段缺失；
- NaN / Infinity / -Infinity；
- 装备数组损坏；
- `lastTs` 损坏；
- journal 超大；
- 正常旧档重新加载并保存。

**验收：**坏档不能无条件覆盖正常档；明确坏档的保护、回退或初始化行为。

---

# 三、P1：当前代码明确存在的问题

## P1-1：Three.js 仍在运行时使用

`bg.js` 仍存在 `import * as THREE from 'three'`，并使用 `WebGLRenderer / Scene / BufferGeometry / ShaderMaterial` 等 WebGL 管线；仓库仍有 `three.module.js`。

**要求：**
- 深空背景改为 Canvas 2D / CSS / 静态背景方案。
- 移除运行时 Three.js 依赖。
- 全仓确认没有运行时 `THREE` / `three.module.js` 引用。
- 确认 `index.html` 不再加载对应入口。
- 验证引用后再删除 `three.module.js` / `bg.js`。
- 不顺手重做游戏主体视觉。

**状态：未完成。**

## P1-2：通用 SFX 仍与 `stage === 1` 耦合

当前 `sfxAudible()` 仍把 `stage === 1` 作为播放条件之一。通用 UI / 系统音效不应因战斗 stage 状态而全部失效。

**要求：**将通用 SFX 条件与战斗专用演出条件分离；保留用户静音、页面不可见、AudioContext 未激活等合理限制。

**状态：仍需修。**

## P1-3：UI 与 DESIGN.md 仍有视觉规范漂移

当前仍可见普通圆角胶囊、`border-radius: 14px / 18px / 999px`、`backdrop-filter` 以及旧霓虹蓝系样式。装备面板已有局部修正，但全局尚未收敛。

**要求：**只做局部收敛，不大改布局，不破坏移动端点击区域。

**状态：部分完成。**

## P1-4：BGM 资源加载链需要确认

`assets/music` 仍有 `bgm.mp3 / bgm2.mp3 / bgm3.mp3 / bgm4.mp3`，总资源约 18 MB。

**要求：**全仓确认静态和动态引用、实际预加载情况。未确认前不要删除资源；如果只有一个 BGM 实际加载，其余列为清理候选。

## P1-5：聚灵阵成本需要数值验证

当前成本公式为 `900 * Math.pow(lv + 1, 2.55)`。

**要求：**按实际允许的最高等级检查最大成本、累计成本、Number 安全整数和 UI 格式化。没有明确问题就不要修改经济曲线。

---

# 四、P1：性能专项复审（本轮新增）

## PERF-1：游戏主循环每帧刷新 HUD —— 明确优化项

`game.js` 主循环每帧执行：

- `tickDsp(dt)`
- `tickAura(dt)`
- `updateHUD()`
- `checkMilestones()`
- 随机 adventure / mainMoment
- `tickBurst(dt)`

其中 `updateHUD()` 会持续执行多个 DOM `textContent` / `style.width` 更新。

**当前问题：**游戏逻辑需要高频运行，但 HUD 没有必要 60 FPS 更新。移动端 WebView 长时间挂机时，这会制造不必要的 DOM 写入和潜在 layout/paint 压力。

**要求：**
- 游戏逻辑仍保持原有 RAF 频率。
- HUD 改为约 **10 FPS（100ms）** 的刷新频率即可。
- 不改变数值逻辑，只降低 UI 刷新频率。
- 破境、装备、聚灵阵等关键事件仍可主动立即刷新 HUD。
- 验证窄屏、低 FPS、前后台切换时不会出现显示滞后或状态错看。

**优先级：P1，建议直接处理。**

## PERF-2：移动端 DPR 统一上限 2，存在 GPU 像素压力

当前 `fx2d.js` 与 `bg.js` 均允许 `devicePixelRatio` 最高到 2。DPR 从 1 增加到 2 时，实际渲染像素面积约增加 4 倍。

当前页面又同时存在：

1. WebGL 深空背景；
2. Canvas 2D 前景特效；
3. 游戏主循环；
4. DOM HUD/UI。

因此低端 Android / 移动 WebView 上可能出现 GPU 占用、发热、掉帧和耗电增加。

**要求：**
- 不要简单粗暴固定所有设备为 DPR 1。
- 优先做设备/屏幕自适应：低端或高分辨率移动设备可使用较低 DPR；桌面和高性能设备保留更高质量。
- 必须保证视觉尺寸、粒子大小和 UI 不发生明显缩放异常。
- 目标是降低实际 framebuffer 像素量，而不是只改 JS 计算频率。

**优先级：P1，建议处理。**

## PERF-3：fx2d 粒子数量目前没有失控，不建议为了“优化”盲目砍粒子

当前最高级特效约数百粒子，且 `softDot()` 有缓存；粒子绘制主要使用缓存贴图 `drawImage()`，结构上可接受。

**结论：**暂不建议直接砍半粒子或重做特效。应先处理 HUD 刷新和 DPR，再用真实移动设备验证。

## PERF-4：WebGL 深空背景粒子规模目前可接受，但与 Canvas 2D 同时渲染需要实机验证

`bg.js` 当前约 770 个星点、250 个银河带点、50 个灵尘以及少量 Sprite / 月亮等，数量本身不构成明显失控。

WebGL 侧已有 `visibilitychange` 后停止 RAF 的省电处理。

**结论：**桌面浏览器预计问题不大；真正风险是低端移动设备上的 **WebGL + Canvas 2D 双渲染**。先实机测，不凭感觉继续砍视觉。

## PERF-5：定时器不是当前性能瓶颈

当前主要低频任务包括：8 秒保存、60 秒云同步检查、60 秒邮件检查、2.5 秒行迹更新、3.5 秒特效诊断。这些本身不是主要性能问题。

不要为了“定时器很多”而进行无意义重构。

## PERF-6：音频资源主要影响首屏网络/内存，不是 FPS 主因

当前 BGM 与多条音效资源总体体积不小。需要确认是否存在不必要的首屏预加载和 AudioBuffer 解码。

**要求：**先确认加载链，再决定延迟加载/删除资源；不要因为性能名义直接删除音乐。

---

# 五、性能实机验收标准

Agent 修改 PERF-1 / PERF-2 后，至少在一台普通 Android 和一台较低性能移动设备验证：

1. 主城正常运行 10 分钟；
2. 高等级粒子特效运行 10 分钟；
3. 自动挂机持续运行；
4. WebGL 背景 + fx2d 同时开启；
5. 切后台 1 分钟再恢复；
6. 快速前后台切换数次；
7. 观察 FPS、明显掉帧、页面发热、内存增长和异常耗电；
8. 检查 HUD 数值是否与实际状态一致。

**不要用“代码看起来没问题”替代实机验证。**

建议目标：
- 普通设备稳定接近 60 FPS；
- 低端设备即使不能稳定 60 FPS，也不得持续恶化或出现明显卡死；
- 后台页面不应持续消耗完整前台渲染功耗；
- 长时间运行不应出现 DOM 节点持续泄漏或粒子数量无限增长。

---

# 六、P2：工程问题

## P2-1：game.js 单文件过大

仍然集中承担大量系统职责。**本轮不要为了性能大拆文件。**只有在后续功能继续增长时再考虑模块化。

## P2-2：版本号 / cache busting 手工同步风险

`GAME_VER`、HTML 显示版本、脚本 query 参数存在手工同步风险。本轮不引入构建系统，只要求修改后核对一致。

## P2-3：生命周期需要回归测试

已有 8 秒保存、`pagehide`、`visibilitychange` 等机制。需实际验证切后台、恢复、关闭页面、浏览器冻结/恢复不会重复离线结算或丢档。

---

# 七、明确暂不处理

1. 后端实现与服务端安全。
2. 没有明确证据的经济平衡调整。
3. 54 段境界模型重新设计。
4. 大规模重构。
5. 全量 UI 重做。
6. 未验证引用关系前删除资源。
7. 为性能盲目降低粒子数量或视觉质量。

---

# 八、Agent 下一轮执行顺序

### A. 先修
- [ ] P0-1：时间跳跃风险处理 / 明确纯前端不可彻底防护边界。
- [ ] P0-2：坏档注入测试与保护。
- [ ] P1-1：移除 Three.js 运行时依赖。
- [ ] P1-2：拆开通用 SFX 与 battle stage 条件。

### B. 性能专项
- [ ] PERF-1：HUD 从每帧刷新降到约 10 FPS，关键事件即时刷新。
- [ ] PERF-2：移动端 DPR 自适应，降低低端设备 framebuffer 压力。
- [ ] PERF-3：不要盲目砍粒子，保持现有视觉，先测再决定。
- [ ] PERF-4：完成 WebGL + fx2d 双渲染移动端实机验证。
- [ ] PERF-6：确认音频资源实际加载/解码链。

### C. 后续
- [ ] P1-3：继续收敛 DESIGN.md 与实际 UI。
- [ ] P1-4：确认 BGM 引用与预加载。
- [ ] P1-5：聚灵阵数值安全验证。
- [ ] P2-3：生命周期回归测试。

### 最终报告必须区分
- **已修复**：代码 + 测试均证明。
- **部分完成**：已有代码改善，但仍有边界。
- **未完成**：实际代码仍存在问题。
- **无需修改**：已检查且没有证据表明存在问题。

---

## 九、v1.7.20 执行记录（2026-09-10 复审后）

> 本节按「以代码为准」核对后落地，非照单全收。改动已本地冒烟验证（Chromium 390×844：主流程 + 6 类坏档注入均无 console 错误）。

### 已落地
- **P1-1（重写）**：`bg.js` 从 Three.js/WebGL 重写为**纯 Canvas 2D**（`洞天·墨夜`：墨色夜空 + 低饱和雾霭 + 视差星点 + 对角银河 + 纸月 + 墨云），配色收敛到 DESIGN.md 色板（去蓝紫霓虹）。删除 `assets/three.module.js` 与 index.html import map；`initBg2D()` 沿用 `initDeepSpace(canvas)` 接口。DPR 低端自适应（触屏/≤4GB 内存 → 1.5，桌面 → 2）；`prefers-reduced-motion` 静止帧；后台停 RAF。
- **PERF-1**：主循环 `loop()` 内 HUD 刷新节流至 ~10FPS（`_hudAcc ≥ 0.1s`）；事件触发仍即时刷新，飘字阈值语义不变。
- **PERF-2**：`fx2d.js` / `#aura` / `bg.js` 三处渲染 DPR 统一改为低端自适应（原固定 cap 2）。
- **P0-1（边界明确）**：`applyOffline()` 增加时钟回拨/未来基准检测——基准落在未来 >60s 时校正并记 `state._warp` 取证，不产生离线收益。**纯前端无法根除持续拨快预支（48h cap/次，需后端权威时钟），已注明**。
- **P1-4**：删除无任何代码引用的 `bgm2/3/4.mp3`（15.7MB → 1.8MB，仅保留唯一加载的 `bgm.mp3`）。
- 版本号四处同步 bump 至 **v1.7.20**（DESIGN §7）。

### 核对后判定不动的项
- **P1-2（误报）**：`sfxAudible() return stage === 1` 是 v1.7.8/v1.7.9 文件头注释明示的「同步铁律」（只有页面可见且战斗/秘境横幅在台才播单发音效，杜绝有声无画）；全仓 SND 调用点均为战斗/横幅演出，无“通用 UI 音效”被误伤。保留原设计。
- **P0-2**：仓库无测试基建；本次以浏览器自动化坏档注入冒烟替代（6 类全过）。是否引入正式测试文件由 owner 定。
- **P1-3/P1-5**：C 组低风险，未动（P1-3 圆角胶囊已清零、backdrop 已强清；P1-5 数值远在安全整数内）。
